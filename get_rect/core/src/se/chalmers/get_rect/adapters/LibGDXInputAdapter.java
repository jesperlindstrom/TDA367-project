package se.chalmers.get_rect.adapters;

import com.badlogic.gdx.Input;

public class LibGDXInputAdapter implements IInputAdapter{

    public LibGDXInputAdapter(Input input) {
        // input.isKeyPressed(Input.Keys.L)
    }
}
